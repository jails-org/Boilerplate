@keyframes fadein
	from {
		opacity 0
		transform translateX(-10px)
	}

	to {
		opacity 1
		transform translateX(0)
	}

@keyframes fadeout
	from {
		opacity 1
		transform translateX(0)
	}

	to {
		opacity 0
		transform translateX(-10px)
	}
