---
const { name, color, class: css } = Astro.props
const icon = await import(`../../../../public/icons/${name}.svg?raw`)
---

<span style=`${color? 'fill:' + color : ''}` class=`${css}`>
	<Fragment set:html={icon.default} />
</span>
