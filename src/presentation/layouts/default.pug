doctype html

block variables
include ./modules.pug

html

	head
		title Boilerplate Jails | #{route.title}
		meta( charset="UTF-8" )
		meta( name="viewport" content="width=device-width, initial-scale=1" )
		link( rel="icon" href="/images/logo-stripes.png" )

		+prevent-alert()
		+dns-prefetch()
		+style()
		+js()
		+fonts()
		+theme()

		block head

	body

		div.site( data-component=`application ${app}` )
			block content

		+third-party()
		+svg-sprite()
